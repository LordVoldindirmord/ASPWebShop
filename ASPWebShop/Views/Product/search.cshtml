@model IEnumerable<WebShop.Domain.ViewModel.ProductModel.ProductViewModel>

@{
    ViewData["Title"] = string.IsNullOrEmpty(ViewBag.SearchTerm)
        ? "Поиск товаров"
        : $"Поиск: {ViewBag.SearchTerm}";
}

<div class="catalog-header">
    <h1>@ViewData["Title"]</h1>
    @if (!string.IsNullOrEmpty(ViewBag.SearchTerm))
    {
        <p>Результаты поиска по запросу «@ViewBag.SearchTerm»</p>
    }
</div>

@if (Model == null || !Model.Any())
{
    <div class="empty-catalog">
        <div class="empty-icon">🔍</div>
        @if (string.IsNullOrEmpty(ViewBag.SearchTerm))
        {
            <h2>Введите поисковый запрос</h2>
            <p>Найдите товары по названию</p>
        }
        else
        {
            <h2>Ничего не найдено</h2>
            <p>По запросу «@ViewBag.SearchTerm» товаров не найдено</p>
            <a asp-action="MainMenu" class="btn-catalog">Вернуться в каталог</a>
        }
    </div>
}
else
{
    <p class="results-count">Найдено: @Model.Count() шт.</p>

    <div class="product-grid">
        @foreach (var product in Model)
        {
            <div class="product-card">
                <div class="product-image">
                    @if (!string.IsNullOrEmpty(product.MainImageUrl))
                    {
                        <img src="@product.MainImageUrl" alt="@product.Name" />
                    }
                    else
                    {
                        <div class="no-image">Нет изображения</div>
                    }
                </div>
                <div class="product-info">
                    <h3 class="product-name">@product.Name</h3>
                    <span class="product-category">@product.CategoryName</span>
                    <p class="product-description">
                        @(product.Description?.Length > 100
                            ? product.Description.Substring(0, 100) + "..."
                            : product.Description)
                    </p>
                    <div class="product-footer">
                        <span class="product-price">@product.Price.ToString("C")</span>
                        <div class="product-actions">
                            <a asp-action="Details" asp-route-id="@product.Id" class="btn-details">Подробнее</a>
                            <form asp-controller="Cart" asp-action="AddToCart" method="post">
                                <input type="hidden" name="productId" value="@product.Id" />
                                <input type="hidden" name="quantity" value="1" />
                                <button type="submit" class="btn-add-to-cart">В корзину</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}